<%= simple_form_for @service_type, :html => { :class => 'form-horizontal' } do |f| %>
  <%= f.input :name %>
  <%= error_span(@service_type[:name]) %>
  <%= f.input :description %>
  <%= error_span(@service_type[:description]) %>

  <%= f.button :submit, :class => 'btn-primary' %>
  <%= link_to t('.cancel', :default => t("helpers.links.cancel")),
                service_types_path, :class => 'btn btn-default' %>
<% end %>
