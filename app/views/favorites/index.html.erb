<%- model_class = Favorite -%>

<table class="table table-striped table-bordered">
  <thead>
    <tr>
     <!-- <th><%= model_class.human_attribute_name(:id) %></th>-->
      <th><%= model_class.human_attribute_name(:Nombre_Favorito) %></th>
      <th><%= model_class.human_attribute_name(:Persona) %></th>
      <th><%= model_class.human_attribute_name(:Dirección) %></th>
     <!-- <th><%= model_class.human_attribute_name(:created_at) %></th>-->
      <th><%=t '.actions', :default => t("helpers.Acciones") %></th>
    </tr>
  </thead>
  <tbody>
    <% @favorites.each do |favorite| %>
      <tr>
     <!--   <td><%= link_to favorite.id, favorite_path(favorite) %></td>-->
        <td><%= favorite.name %></td>
        <td><%= favorite.person.name %></td><!--linea-->
        <td><%= favorite.address.name %></td><!--linea-->
     <!--   <td><%=l favorite.created_at %></td>-->
        <td>
          <%= link_to t('.edit', :default => t("helpers.links.edit")),
                      edit_favorite_path(favorite), :class => 'btn btn-default btn-xs' %>
          <%= link_to t('.destroy', :default => t("helpers.links.destroy")),
                      favorite_path(favorite),
                      :method => :delete,
                      :data => { :confirm => t('.confirm', :default => t("helpers.links.confirm", :default => 'Are you sure?')) },
                      :class => 'btn btn-xs btn-danger' %>
        </td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to t('.new', :default => t("helpers.links.new")),
            new_favorite_path,
            :class => 'btn btn-primary' %>
